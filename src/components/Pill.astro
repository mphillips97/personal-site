<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0 0.5rem;
		gap: 0.5rem;
		color: var(--gray-0);
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
	}
</style>
